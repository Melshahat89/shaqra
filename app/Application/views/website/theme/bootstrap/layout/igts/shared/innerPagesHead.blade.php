<section class="sec sec_pad_top sec_pad_bottom bg_gradient {{ (isMobile()) ? '' : 'sticky-stopper' }}">
    <div class="wrapper">
        <section class="title mblg">
            <h2 class="text_white text_capitalize">{{$title}}</h2>
            <p style="color: white;">{{isset($subTitle) ? $subTitle : ''}}</p>
        </section>
    </div>
</section>