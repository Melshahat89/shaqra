<?php

namespace App\Application\DataTables;

use App\Application\Model\Searchkeys;
use Yajra\DataTables\Services\DataTable;

class SearchkeyssDataTable extends DataTable
{
    /**
     * Display ajax response.
     *
     * @return \Illuminate\Http\JsonResponse
     */
    public function ajax()
    {
        return datatables()
             ->eloquent($this->query())
              ->addColumn('id', 'admin.searchkeys.buttons.id')
             ->addColumn('edit', 'admin.searchkeys.buttons.edit')
             ->addColumn('delete', 'admin.searchkeys.buttons.delete')
             ->addColumn('view', 'admin.searchkeys.buttons.view')
             /*->addColumn('name', 'admin.searchkeys.buttons.langcol')*/
             ->rawColumns(['id', 'edit', 'delete', 'view'])
             ->make(true);
    }
    /**
     * Get the query object to be processed by dataTables.
     *
     * @return \Illuminate\Database\Eloquent\Builder|\Illuminate\Database\Query\Builder|\Illuminate\Support\Collection
     */
    public function query()
    {
        $query = Searchkeys::query();

        if(request()->has('from') && request()->get('from') != ''){
            $query = $query->whereDate('created_at' , '>=' , request()->get('from'));
        }

        if(request()->has('to') && request()->get('to') != ''){
            $query = $query->whereDate('created_at' , '<=' , request()->get('to'));
        }

		if(request()->has("word") && request()->get("word") != ""){
				$query = $query->where("word","=", request()->get("word"));
		}

		if(request()->has("ip") && request()->get("ip") != ""){
				$query = $query->where("ip","=", request()->get("ip"));
		}

		if(request()->has("country") && request()->get("country") != ""){
				$query = $query->where("country","=", request()->get("country"));
		}

		if(request()->has("city") && request()->get("city") != ""){
				$query = $query->where("city","=", request()->get("city"));
		}



        return $this->applyScopes($query);
    }

    /**
     * Optional method if you want to use html builder.
     *
     * @return \Yajra\Datatables\Html\Builder
     */
    public function html()
    {
        return $this->builder()
                    ->columns($this->getColumns())
                    ->parameters(dataTableConfig());
    }
    /**
     * Get columns.
     *
     * @return array
     */
    protected function getColumns()
    {
        return [
              [
                  'name' => "id",
                  'data' => 'id',
                  'title' => trans('curd.id'),
             ],
			[
                'name' => 'word',
                'data' => 'word',
                'title' => "word",
                
                ],
             [
                  'name' => 'view',
                  'data' => 'view',
                  'title' => trans('curd.view'),
                  'exportable' => false,
                  'printable' => false,
                  'searchable' => false,
                  'orderable' => false,
             ],
             [
                  'name' => 'edit',
                  'data' => 'edit',
                  'title' =>  trans('curd.edit'),
                  'exportable' => false,
                  'printable' => false,
                  'searchable' => false,
                  'orderable' => false,
             ],
             [
                   'name' => 'delete',
                   'data' => 'delete',
                   'title' => trans('curd.delete'),
                   'exportable' => false,
                   'printable' => false,
                   'searchable' => false,
                   'orderable' => false,
             ],

        ];
    }

    /**
     * Get filename for export.
     *
     * @return string
     */
    protected function filename()
    {
        return 'Searchkeysdatatables_' . time();
    }
}