<?php

namespace App\Application\DataTables;

use App\Application\Model\Sectionquizchoise;
use Yajra\DataTables\Services\DataTable;

class SectionquizchoisesDataTable extends DataTable
{
    /**
     * Display ajax response.
     *
     * @return \Illuminate\Http\JsonResponse
     */
    public function ajax()
    {
        return datatables()
             ->eloquent($this->query())
              ->addColumn('id', 'admin.sectionquizchoise.buttons.id')
             ->addColumn('edit', 'admin.sectionquizchoise.buttons.edit')
             ->addColumn('delete', 'admin.sectionquizchoise.buttons.delete')
             ->addColumn('view', 'admin.sectionquizchoise.buttons.view')
             /*->addColumn('name', 'admin.sectionquizchoise.buttons.langcol')*/
             ->rawColumns(['id', 'edit', 'delete', 'view'])

            ->make(true);
    }
    /**
     * Get the query object to be processed by dataTables.
     *
     * @return \Illuminate\Database\Eloquent\Builder|\Illuminate\Database\Query\Builder|\Illuminate\Support\Collection
     */
    public function query()
    {
        $query = Sectionquizchoise::query();

        if(request()->has('from') && request()->get('from') != ''){
            $query = $query->whereDate('created_at' , '>=' , request()->get('from'));
        }

        if(request()->has('to') && request()->get('to') != ''){
            $query = $query->whereDate('created_at' , '<=' , request()->get('to'));
        }

		if(request()->has("choise") && request()->get("choise") != ""){
				$query = $query->where("choise","=", request()->get("choise"));
		}

		if(request()->has("is_correct") && request()->get("is_correct") != ""){
				$query = $query->where("is_correct","=", request()->get("is_correct"));
		}



        return $this->applyScopes($query);
    }

    /**
     * Optional method if you want to use html builder.
     *
     * @return \Yajra\Datatables\Html\Builder
     */
    public function html()
    {
        return $this->builder()
                    ->columns($this->getColumns())
                    ->parameters(dataTableConfig());
    }
    /**
     * Get columns.
     *
     * @return array
     */
    protected function getColumns()
    {
        return [
              [
                  'name' => "id",
                  'data' => 'id',
                  'title' => trans('curd.id'),
             ],
			[
                'name' => 'choise',
                'data' => 'choise',
                'title' => "choise",
                
                ],
             [
                  'name' => 'view',
                  'data' => 'view',
                  'title' => trans('curd.view'),
                  'exportable' => false,
                  'printable' => false,
                  'searchable' => false,
                  'orderable' => false,
             ],
             [
                  'name' => 'edit',
                  'data' => 'edit',
                  'title' =>  trans('curd.edit'),
                  'exportable' => false,
                  'printable' => false,
                  'searchable' => false,
                  'orderable' => false,
             ],
             [
                   'name' => 'delete',
                   'data' => 'delete',
                   'title' => trans('curd.delete'),
                   'exportable' => false,
                   'printable' => false,
                   'searchable' => false,
                   'orderable' => false,
             ],

        ];
    }

    /**
     * Get filename for export.
     *
     * @return string
     */
    protected function filename()
    {
        return 'Sectionquizchoisedatatables_' . time();
    }
}